#include <bits/stdc++.h>
using namespace std;

/*notas:

1. a maquina começa desligada
2. numeros impares MUDAM o estado(nao apenas ligam nem apenas desligam)
3. a melhor soluçao sempre sera (eu acho): ligar no maior impar (e logo depois coletar esse impar),
colher todos os pares, desligar no menor impar, ligar no segundo maior impar, desligar no segundo menor impar, ate terminar os campos
*/

int main(){
    iostream::sync_with_stdio(false);
    cin.tie(nullptr);
    
    short testcases;
    int campos;

    int* impares = new int[campos];
    int* pares = new int[campos];
    
    cout << "nao terminei ainda";

    return 0;
};